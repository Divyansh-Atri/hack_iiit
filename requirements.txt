# Teams Speaker Recognition Backend
# Python 3.11+ (tested with 3.11, 3.12, 3.13)

# Web framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
websockets==12.0

# Audio processing
sounddevice==0.4.6
numpy>=1.26.0,<3.0.0
# scipy: For Python 3.13, use system package (python3-scipy) if available
# scipy 1.12.0 is incompatible with Python 3.13 + numpy 2.x
# The install script will handle this automatically

# Deep learning
# PyTorch: Versions will be handled by install script based on Python version
# Python 3.13 requires torch>=2.5.0 (no wheels for 2.1.2 on 3.13)
# For Python < 3.13: torch==2.1.2, torchaudio==2.1.2
# For Python 3.13: torch>=2.5.0, torchaudio>=2.5.0
torch
torchaudio
speechbrain==0.5.16

# Utilities
python-multipart==0.0.6
requests>=2.31.0
